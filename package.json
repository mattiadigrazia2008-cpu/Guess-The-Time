{
  "name": "guess-the-time",
  "version": "1.0.0",
  "description": "Guess the Time - app di precisione sul tempo per 2-4 giocatori",
  "private": true,
  "scripts": {
    "android": "npx cap add android && npx cap copy android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
